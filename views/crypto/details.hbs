<main>
    <section id="details-info">
        <h1>Details</h1>
        <div class="coin-image">
            <img src="{{image}}">
        </div>

        <div class="coin-info">
            <div class="coin-text">
                <h1 id="name">{{name}}</h1>
                <h3 id="payment">Payment method: {{paymentMethod}}</h3>
                <p id="price"><span>Price: {{price}}</span></p>
                <p id="description">{{description}}</p>
            </div>

            {{#if user}}
            <div class="product-btn">
                {{#if isAuthor}}
                <!--Only for registered user and author of the publication -->
                <div class="author">
                    <a href="/crypto/{{_id}}/edit" class="btn-edit">Edit</a>
                    <a href="/crypto/{{_id}}/delete" class="btn-delete">Delete</a>
                </div>
                {{else}}
                {{#if isAlreadyJoin}}
                <!-- Logged in user who has already buy this crypto-->
                <p class="buy-message">You already bought these crypto coins.</p>
                {{else}}
                <!-- Logged in user who has not yet buy this crypto-->
                <a href="/crypto/{{_id}}/join" class="btn-buy">Buy</a>
                {{/if}}
                {{/if}}

            </div>

            {{/if}}
        </div>
    </section>
</main>










{{!-- <section id="viewhotelDetails">
    <h2>Details</h2>
    <div class="hotel-ticket">
        <div class="hotel-left">
            <img src="{{imgUrl}}" alt="imgUrl">
        </div>
        <div class="hotel-right">
            <div>
                <h3>{{hotel}}</h3>
            </div>
            <div>
                {{city}}
            </div>
            <p><span>Free rooms: {{freeRooms}}</span> </p>



            {{#if isAuthor}}
            <a href="/booking/{{_id}}/edit" class="edit">Edit</a>
            <a href="/booking/{{_id}}/delete" class="remove">Delete</a>
            {{else}}
            {{#if isAlreadyJoin}}
            <p><span class="green">You already have booked a room</span> </p>
            {{else}}
            <a href="/booking/{{_id}}/join" class="book">Book</a>
            {{/if}}
            {{/if}}
        </div>

    </div>
</section> --}}